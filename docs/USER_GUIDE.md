# 使用说明

- 启动：双击 `启动 Local Video Transcriber.command`。
- 停止：运行安装目录中的 `scripts/stop.command`。
- 提交：在插件中每行填写一个 HTTP 或 HTTPS 视频 URL。
- 下载：任务完成后选择单个产物下载；“每次选择”会在下载前打开保存窗口。
- 自动配对：Token 由每台 Mac 独立生成，只保存在本机后端配置和 Chrome 扩展存储中。
